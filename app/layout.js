import './globals.css';

export const metadata = {
  title: 'RootCause AI',
  description: 'Distributed system intelligence demo'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
